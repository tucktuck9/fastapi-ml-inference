# Public URL of the backend — must be reachable from the browser, not the Docker network
# On Render this is injected automatically via fromService → RENDER_EXTERNAL_URL
BACKEND_URL=http://localhost:8000
# Port the uvicorn server listens on
PORT=3000
# Disable Python stdout buffering for real-time log streaming in containerized environments
PYTHONUNBUFFERED=1