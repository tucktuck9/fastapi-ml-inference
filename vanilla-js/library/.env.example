# Postgres connection string — on Render injected automatically via fromDatabase
# Locally docker-compose sets this to point at the postgres service
DATABASE_URL=postgresql://library:library@postgres:5432/library
# Port the uvicorn server listens on
PORT=8001
# Disable Python stdout buffering for real-time log streaming in containerized environments
PYTHONUNBUFFERED=1
