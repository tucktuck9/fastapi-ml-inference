# Vite reads VITE_-prefixed variables at build time and replaces references to
# `import.meta.env.VITE_*` in the bundled JS. 

# On Render this is injected automatically via fromService → RENDER_EXTERNAL_URL.
VITE_BACKEND_URL=http://localhost:8000